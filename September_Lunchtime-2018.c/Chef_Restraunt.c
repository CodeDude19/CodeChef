#include<stdio.h>

int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
      int a,b;
      scanf("%d%d",&a,&b);
      long int arr1[a*2],arr2[b];
      for(int i =0;i<a*2;i++)
      {
        scanf("%d",&arr1[i]);
      }
      for(int i=0;i<b;i++)
      {
        scanf("%d",&arr2[i]);
      }
      for(int i=0;i<b;i++)
      {
        for(int j=0;j<a;j++)
        {
          if(arr1[])
        }
      }

    }
}
